import{Database,GitBranch,Link2,Palette,Tags}from"lucide-react";import{importMasterdataFile}from"@/app/actions";import{PageHeader}from"@/components/ui";import{requireOperator}from"@/lib/club-context";import{getMasterdataSummary}from"@/lib/data";
export const dynamic="force-dynamic";export default async function Page({searchParams}:{searchParams:Promise<Record<string,string|undefined>>}){await requireOperator();const[summary,query]=await Promise.all([getMasterdataSummary(),searchParams]),message=query.checked?`${query.checked} Zeilen erfolgreich geprüft.`:query.created?`Import abgeschlossen: ${query.created} neu, ${query.updated} aktualisiert, ${query.unchanged} unverändert.`:query.error?"Die Datei konnte nicht verarbeitet werden.":null,cards=[{label:"Kategorien",value:summary.categories,icon:Database},{label:"Rassegruppen",value:summary.groups,icon:GitBranch},{label:"Rassen",value:summary.breeds,icon:Tags},{label:"Farbenschläge",value:summary.colors,icon:Palette},{label:"Kombinationen",value:summary.combinations,icon:Link2}];return <><PageHeader title="BDRG-Stammdaten" description={`Stand: ${summary.sourceVersion??"noch nicht importiert"}`}/><section className="stats masterdata-stats">{cards.map(({label,value,icon:Icon})=><div className="stat-card" key={label}><Icon/><div><strong>{value}</strong><span>{label}</span></div></div>)}</section><p className="sub">Letzte Aktualisierung: {summary.lastImport?new Date(summary.lastImport).toLocaleString("de-DE"):"noch nie"}</p>{message?<p className={query.error?"form-error":"form-success"}>{message}</p>:null}<form action={importMasterdataFile} className="form-card masterdata-import"><h2>BDRG-Aktualisierungsimport</h2><p>CSV oder JSON auswählen. Der Import verwendet eindeutige Schlüssel und erzeugt keine Dubletten.</p><input type="file" name="file" accept=".csv,.json,text/csv,application/json" required/><div className="form-actions"><button className="button secondary" name="mode" value="check">Import prüfen</button><button className="button primary" name="mode" value="import">Import ausführen</button></div></form></>}
