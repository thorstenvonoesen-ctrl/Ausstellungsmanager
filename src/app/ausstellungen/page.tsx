import { requireActiveClub } from "@/lib/club-context";import { getExhibitions } from "@/lib/data";import { EmptyState,PageHeader,RowActions,Status } from "@/components/ui";
export const dynamic="force-dynamic";export default async function Page(){const club=await requireActiveClub();const rows=await getExhibitions(club.id);return <><PageHeader title="Ausstellungen" description={`Veranstaltungen von ${club.name}`} actionHref="/ausstellungen/neu" actionLabel="Neue Ausstellung"/>{rows.length===0?<EmptyState title="Noch keine Ausstellung" text="Legen Sie die erste Ausstellung an." href="/ausstellungen/neu" label="Ausstellung anlegen"/>:<div className="table-card"><div className="table-wrap"><table><thead><tr><th>Ausstellung</th><th>Zeitraum</th><th>Meldeschluss</th><th>Status</th><th/></tr></thead><tbody>{rows.map(e=><tr key={e.id}><td><span className="primary-cell">{e.title}</span><span className="sub">{e.venue}</span></td><td>{new Date(e.starts_at).toLocaleDateString("de-DE")} – {new Date(e.ends_at).toLocaleDateString("de-DE")}</td><td>{new Date(e.registration_deadline).toLocaleDateString("de-DE")}</td><td><Status>{e.status}</Status></td><td><RowActions editHref={`/ausstellungen/${e.id}/bearbeiten`} id={e.id} kind="exhibition"/></td></tr>)}</tbody></table></div></div>}</>}
