import{notFound}from"next/navigation";import{ExhibitionForm}from"@/components/exhibition-form";import{FormHeader}from"@/components/ui";import{requireActiveClub}from"@/lib/club-context";import{getExhibition,getShowFees}from"@/lib/data";
export const dynamic="force-dynamic";export default async function Page({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string}>}){const[{id},club,{error}]=await Promise.all([params,requireActiveClub(),searchParams]),[item,fees]=await Promise.all([getExhibition(id,club.id),getShowFees(id,club.id)]);if(!item)notFound();return <div className="form-shell"><FormHeader title="Ausstellung bearbeiten" back="/ausstellungen"/><ExhibitionForm club={club} item={item} fees={fees} error={error}/></div>}
