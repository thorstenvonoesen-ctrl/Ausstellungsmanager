import Link from"next/link";import{notFound}from"next/navigation";import{deleteShowFee,deleteShowSection,saveShowFee,saveShowSection}from"@/app/actions";import{FormHeader}from"@/components/ui";import{requireActiveClub}from"@/lib/club-context";import{getExhibition,getShowFees,getShowSections}from"@/lib/data";
const labels:Record<string,string>={animal:"Einzeltier",aviary:"Voliere",stem:"Stamm",expenses:"Unkostenbeitrag",catalog:"Katalog",other:"Weitere Kostenposition / Eintritt / Spende"};
export const dynamic="force-dynamic";export default async function Page({params,searchParams}:{params:Promise<{id:string}>;searchParams:Promise<{error?:string}>}){const[{id},club,{error}]=await Promise.all([params,requireActiveClub(),searchParams]),[show,fees,sections]=await Promise.all([getExhibition(id,club.id),getShowFees(id,club.id),getShowSections(id,club.id)]);if(!show)notFound();return <div className="form-shell"><FormHeader title={`Gebühren · ${show.title}`} back="/ausstellungen"/>{error?<p className="form-error">Die Gebührenposition konnte nicht gespeichert werden.</p>:null}<section className="form-card fee-management"><h2>Gebühren dieser Ausstellung</h2>{fees.length===0?<p className="sub">Noch keine Gebühren hinterlegt.</p>:<div className="fee-list">{fees.map(fee=><div key={fee.id}><span><strong>{fee.name}</strong><small>{labels[fee.fee_type]??fee.fee_type}{fee.mandatory?" · Pflichtposition":" · optional"}</small></span><b>{Number(fee.amount).toLocaleString("de-DE",{style:"currency",currency:"EUR"})}{fee.youth_amount!==null?<small> · Jugend {Number(fee.youth_amount).toLocaleString("de-DE",{style:"currency",currency:"EUR"})}</small>:null}</b>{fee.active?<form action={deleteShowFee}><input type="hidden" name="showId" value={id}/><input type="hidden" name="feeId" value={fee.id}/><button className="button secondary" type="submit">Deaktivieren</button></form>:<em>inaktiv</em>}</div>)}</div>}</section><form action={saveShowFee} className="form-card form-grid"><input type="hidden" name="showId" value={id}/><h2 className="wide">Kostenposition hinzufügen</h2><label className="field"><span>Bezeichnung *</span><input name="name" required placeholder="z. B. Meldegebühr Einzeltier"/></label><label className="field"><span>Typ *</span><select name="feeType" required>{Object.entries(labels).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select></label><label className="field"><span>Preis Erwachsene (€) *</span><input name="amount" type="number" min="0" step="0.01" required/></label><label className="field"><span>Jugendpreis (€), optional</span><input name="youthAmount" type="number" min="0" step="0.01"/></label><label className="terms wide"><input type="checkbox" name="mandatory"/>Automatisch berechnen (Pflichtposition)</label><div className="form-actions"><button className="button primary" type="submit">Kostenposition speichern</button></div></form><section className="form-card fee-management"><h2>Ausstellungsarten / Abteilungen</h2><p className="sub">Nur aktive Bereiche werden im öffentlichen Meldeformular angeboten.</p><div className="fee-list">{sections.map(section=><div key={section.id}><strong>{section.name}</strong><span/><form action={deleteShowSection}><input type="hidden" name="showId" value={id}/><input type="hidden" name="sectionId" value={section.id}/><button className="button secondary" type="submit">Deaktivieren</button></form></div>)}</div><form action={saveShowSection} className="toolbar"><input type="hidden" name="showId" value={id}/><input className="search" name="name" required placeholder="z. B. Allgemeine Schau"/><button className="button primary" type="submit">Bereich hinzufügen</button></form></section><Link className="button secondary" href="/ausstellungen">Zur Ausstellungsübersicht</Link></div>}
