import Link from"next/link";import{notFound}from"next/navigation";import{ArrowLeft,Pencil}from"lucide-react";import{requireActiveClub}from"@/lib/club-context";import{getExhibitor}from"@/lib/data";
export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){const[{id},club]=await Promise.all([params,requireActiveClub()]);const x=await getExhibitor(id,club.id);if(!x)notFound();return <><div className="detail-actions"><Link className="button secondary" href="/aussteller"><ArrowLeft size={17}/>Zur Liste</Link><Link className="button primary" href={`/aussteller/${id}/bearbeiten`}><Pencil size={16}/>Bearbeiten</Link></div><div className="page-header"><div><h1>{x.first_name} {x.last_name}</h1><p>{x.association_name||x.club_name}</p></div></div><section className="detail-grid"><div className="detail-card"><h2>Kontaktdaten</h2><div className="facts"><div className="fact"><span>Anschrift</span><strong>{x.street}<br/>{x.postal_code} {x.city}</strong></div><div className="fact"><span>E-Mail</span><strong>{x.email||"–"}</strong></div><div className="fact"><span>Telefon</span><strong>{x.phone||"–"}</strong></div></div></div><aside className="detail-card"><h2>Zuordnung</h2><div className="fact"><span>Ausstellung</span><strong>{x.show_title}</strong></div></aside></section></>}
