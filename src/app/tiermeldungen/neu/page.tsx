import{EntryForm}from"@/components/entry-form";import{FormHeader}from"@/components/ui";import{getBreedVariants,getExhibitions,getExhibitors}from"@/lib/data";
export const dynamic="force-dynamic";export default async function Page(){const[exhibitions,exhibitors,variants]=await Promise.all([getExhibitions(),getExhibitors(),getBreedVariants()]);return <div className="form-shell"><FormHeader title="Neue Tiermeldung" back="/tiermeldungen"/>{exhibitions.length&&exhibitors.length&&variants.length?<EntryForm exhibitions={exhibitions} exhibitors={exhibitors} variants={variants}/>:<p>Bitte legen Sie zuerst eine Ausstellung, einen Aussteller und mindestens eine aktive Rasse/Farbvariante an.</p>}</div>}
