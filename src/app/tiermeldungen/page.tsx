import Link from"next/link";import{requireActiveClub}from"@/lib/club-context";import{getEntries,getExhibitions}from"@/lib/data";import{EmptyState,PageHeader,RowActions,Status}from"@/components/ui";
export const dynamic="force-dynamic";export default async function Page({searchParams}:{searchParams:Promise<{q?:string;ausstellung?:string}>}){const club=await requireActiveClub(),{q="",ausstellung=""}=await searchParams,[rows,exhibitions]=await Promise.all([getEntries(club.id,q,ausstellung),getExhibitions(club.id)]);return <><PageHeader title="Tiermeldungen" description={`Eingegangene Tiermeldungen für ${club.name}`}/><form className="toolbar"><input className="search" name="q" placeholder="Rasse, Ringnummer oder Aussteller suchen …" defaultValue={q}/><select name="ausstellung" defaultValue={ausstellung}><option value="">Alle Ausstellungen</option>{exhibitions.map(e=><option key={e.id} value={e.id}>{e.title}</option>)}</select><button className="button secondary">Filtern</button>{q||ausstellung?<Link className="button secondary" href="/tiermeldungen">Zurücksetzen</Link>:null}</form>{rows.length===0?<EmptyState title="Keine Tiermeldungen" text="Noch keine öffentlichen Meldungen eingegangen."/>:<div className="table-card"><div className="table-wrap"><table><thead><tr><th>Tier</th><th>Aussteller</th><th>Ausstellung</th><th>Ring / Käfig</th><th>Gebühr</th><th>Status</th><th/></tr></thead><tbody>{rows.map(a=><tr key={a.id}><td><span className="primary-cell">{a.breed}</span><span className="sub">{a.species} · {a.color||"ohne Farbenschlag"} · {a.sex}</span></td><td>{a.exhibitor_name}</td><td>{a.exhibition_title}</td><td>{a.ring_number}<span className="sub">Käfig {a.cage_number||"–"}</span></td><td>{Number(a.entry_fee).toLocaleString("de-DE",{style:"currency",currency:"EUR"})}</td><td><Status>{a.status}</Status></td><td><RowActions editHref={`/tiermeldungen/${a.id}/bearbeiten`} id={a.id} kind="entry"/></td></tr>)}</tbody></table></div></div>}</>}
