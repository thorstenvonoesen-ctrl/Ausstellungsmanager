import Link from"next/link";import{getClubs}from"@/lib/data";import{EmptyState,PageHeader,RowActions}from"@/components/ui";
export const dynamic="force-dynamic";export default async function Page(){const rows=await getClubs();return <><PageHeader title="Vereine" description="Mandanten und Stammdaten der veranstaltenden Vereine" actionHref="/vereine/neu" actionLabel="Neuer Verein"/>{rows.length===0?<EmptyState title="Noch kein Verein" text="Ein Verein ist Voraussetzung für Ausstellungen und Aussteller." href="/vereine/neu" label="Verein anlegen"/>:<div className="table-card"><div className="table-wrap"><table><thead><tr><th>Verein</th><th>Ansprechpartner</th><th>Kontakt</th><th/></tr></thead><tbody>{rows.map(c=><tr key={c.id}><td><Link className="primary-cell" href={`/vereine/${c.id}`}>{c.name}</Link><span className="sub">{c.short_name}</span></td><td>{c.contact_person||"–"}</td><td>{c.email||c.phone||"–"}</td><td><RowActions editHref={`/vereine/${c.id}/bearbeiten`} id={c.id} kind="club"/></td></tr>)}</tbody></table></div></div>}</>}
