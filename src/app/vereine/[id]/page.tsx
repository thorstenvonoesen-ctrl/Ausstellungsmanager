import Link from"next/link";import{notFound}from"next/navigation";import{ArrowLeft,Pencil}from"lucide-react";import{getClub}from"@/lib/data";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const c=await getClub(id);if(!c)notFound();return <><div className="detail-actions"><Link className="button secondary" href="/vereine"><ArrowLeft size={17}/>Zur Liste</Link><Link className="button primary" href={`/vereine/${id}/bearbeiten`}><Pencil size={16}/>Bearbeiten</Link></div><div className="page-header"><div><h1>{c.name}</h1><p>{c.short_name}</p></div></div><div className="detail-card"><h2>Vereinsdaten</h2><div className="facts"><div className="fact"><span>Anschrift</span><strong>{c.address||"–"}</strong></div><div className="fact"><span>Ansprechpartner</span><strong>{c.contact_person||"–"}</strong></div><div className="fact"><span>E-Mail</span><strong>{c.email||"–"}</strong></div><div className="fact"><span>Telefon</span><strong>{c.phone||"–"}</strong></div></div></div></>}
