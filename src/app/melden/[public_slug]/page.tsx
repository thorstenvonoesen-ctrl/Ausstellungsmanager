import{notFound}from"next/navigation";import{PublicEntryForm}from"@/components/public-entry-form";import{getMasterdataHierarchy,getPublicShow}from"@/lib/data";
export const dynamic="force-dynamic";export default async function Page({params,searchParams}:{params:Promise<{public_slug:string}>;searchParams:Promise<{error?:string}>}){const{public_slug}=await params,{error}=await searchParams,show=await getPublicShow(public_slug);if(!show)notFound();const open=show.club_active&&show.status==="registration_open"&&new Date(show.registration_deadline)>=new Date();if(!open)return <div className="public-registration"><h1>{show.title}</h1><div className="empty"><h3>Der Meldeschluss für diese Ausstellung ist bereits erreicht.</h3><p>Neue Meldungen sind nicht mehr möglich.</p></div></div>;const data=await getMasterdataHierarchy(),message=error==="validation"?"Bitte prüfen Sie Ihre Angaben und alle Tiere.":error==="variant"?"Eine gewählte Rasse/Farbenschlag-Zuordnung ist nicht mehr verfügbar.":error?"Die Meldung konnte nicht gespeichert werden. Bitte versuchen Sie es erneut.":null;return <div className="public-registration"><header className="public-show-head"><span>Öffentliche Tiermeldung · {show.club_name}</span><h1>{show.title}</h1><p>{show.venue} · Meldeschluss {new Date(show.registration_deadline).toLocaleString("de-DE")}</p></header>{message?<p className="form-error">{message}</p>:null}<PublicEntryForm slug={public_slug} data={data}/></div>}
