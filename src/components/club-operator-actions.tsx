import Link from"next/link";import{CheckCircle2,Pencil,Power}from"lucide-react";import{deactivateClub,selectActiveClub}from"@/app/actions";
export function ClubOperatorActions({id,active}:{id:string;active:boolean}){return <div className="row-actions"><Link className="icon-button" href={`/vereine/${id}/bearbeiten`} title="Bearbeiten"><Pencil size={16}/></Link>{active?<><form action={selectActiveClub}><input type="hidden" name="clubId" value={id}/><button className="icon-button" title="Verein auswählen" aria-label="Verein auswählen"><CheckCircle2 size={16}/></button></form><form action={deactivateClub}><input type="hidden" name="id" value={id}/><button className="icon-button danger" title="Verein deaktivieren" aria-label="Verein deaktivieren"><Power size={16}/></button></form></>:null}</div>}
